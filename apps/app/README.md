# yuyan_app

Yuyan（语燕）AI 宠物应用 Flutter 客户端。

当前客户端服务于 AI 宠物陪伴闭环：登录、宠物主页、文本对话、记忆、成长状态和订阅权益展示。具体产品与架构规范见仓库根目录的 `docs/LLM_DEV_GUIDE.md`。

## Getting Started

```bash
flutter pub get
dart run build_runner build --delete-conflicting-outputs
flutter test
flutter run
```
